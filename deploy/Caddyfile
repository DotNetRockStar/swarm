# Replace with your real domain before deploying.
swarm.example.com {
	encode zstd gzip
	# WSS and REST both proxy to the same Axum listener; Caddy upgrades
	# websockets automatically.
	reverse_proxy stun-server:8080
}

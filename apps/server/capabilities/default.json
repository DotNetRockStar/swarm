{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Baseline capabilities for the SWARM Server main window — this app's own commands only; the folder picker is called from Rust, not JS, so no dialog permission is needed here.",
  "windows": ["main"],
  "permissions": ["core:default"]
}

{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Baseline capabilities for the SWARM Server main window — this app's own commands only; the folder picker and the info modal's external links are both called from Rust (via open_external_url), not JS, so no dialog/opener plugin permission is needed here.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
